package seedu.addressbook.data.person;

/**
 * An interface for any contact field with a printable string.
 */
public interface Printable {

    /**
     * Returns a printable string for a list of contact fields
     */
    public String getPrintableString();
}
