package seedu.addressbook.data.person;

/**
 * An interface to get the values classes: Name, Phone, Email, Address
 */
public interface Printable {
    /* Produces printable string representation */
    public String getPrintableString();
}
